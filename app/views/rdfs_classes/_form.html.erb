<%= form_with(model: rdfs_class, local: true) do |form| %>
  <% if rdfs_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rdfs_class.errors.count, "error") %> prohibited this rdfs_class from being saved:</h2>

      <ul>
      <% rdfs_class.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :rdfs_class_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
