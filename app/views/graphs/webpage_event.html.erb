<%= debug JSON.parse(@google_jsonld) %>

<hr>


<%= link_to 'export to S3', databus_create_path(
  jsonld: @google_jsonld, 
  artifact: params[:seedurl],  
  version:  Date.today.iso8601, 
  file: "#{params[:url]}.json"
  ),  method: :post 
%>

