<%= form_with(model: search_exception, local: true) do |form| %>
  <% if search_exception.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(search_exception.errors.count, "error") %> prohibited this search_exception from being saved:</h2>

      <ul>
      <% search_exception.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :search_exception_name %>
  </div>

  <div class="field" style="display:float !important">
    <%= form.label :rdfs_class_id %>
   
      <%= form.collection_select :rdfs_class_id, RdfsClass.all, :id, :name %>
    
    </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
