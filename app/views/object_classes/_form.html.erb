<%= form_with(model: object_class, local: true) do |form| %>
  <% if object_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(object_class.errors.count, "error") %> prohibited this object_class from being saved:</h2>

      <ul>
      <% object_class.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :object_class_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
