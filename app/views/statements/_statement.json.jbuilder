json.extract! statement, :id, :status_id, :predicate_id, :webpage_id, :cache, :status_origin, :cache_refreshed, :cache_changed, :created_at, :updated_at
json.url statement_url(statement, format: :json)
